pipeline {
    agent any

    tools{
        maven  "M3"
    }
    stages{
        stage ('Build'){
	   		sh "mvn clean install"
            dir("comtest/target") {
                sh "java -jar testing-1.0-SNAPSHOT.jar"
            }
	    }
    }

}
